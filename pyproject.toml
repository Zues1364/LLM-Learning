[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-learning-deploy-bootstrap"
version = "0.0.0"
description = "Deployment bootstrap hooks for LLM Learning."
requires-python = ">=3.11"

[tool.setuptools]
py-modules = ["sitecustomize"]
